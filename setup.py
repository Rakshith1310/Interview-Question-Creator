from setuptools import setup, find_packages #find_packages() returns a list of packages in the current directory

setup(
    name = "Gen AI project",
    version = "0.0.0",
    author = "Rakshith",
    packages = find_packages(),
    install_requires = []
)